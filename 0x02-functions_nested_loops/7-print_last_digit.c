#include "main.h"

/**
 * _abs - Computes the absolute value
 * of an integer.
 *
 * @i: input number either positive or negative
 *
 * Return: absolute value
 */
int _abs(int i)
{
	for (i >= 0);
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
